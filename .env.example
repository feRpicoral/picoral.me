PUBLIC_SITE_URL=https://picoral.me

# Required only when running `pnpm translate` locally to (re)generate pt/es
# translations from src/content/<collection>/en/*. CI and Vercel never need
# this — they only run `pnpm translate:check`, which makes no API calls.
ANTHROPIC_API_KEY=
